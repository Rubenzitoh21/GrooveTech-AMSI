<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Theme.GrooveTech.NoActionBar" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/MainThemeColor</item>
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
        //disable action bar
        <item name="android:windowActionBar">false</item>
    </style>

    <!-- Text Styles -->
    <style name="textStyle" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="textStyleBold" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- SearchView Styles -->
    <style name="SearchViewTheme" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorHint">@color/text_color</item>
        <item name="queryHint">"Procurar produtos..."</item>
    </style>
</resources>


